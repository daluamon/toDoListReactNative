import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    backgroundColor: "#262626",
    marginHorizontal: 30,
    padding: 12,
    flexDirection: "row",
    gap: 8,
    marginBottom: 12,
    borderRadius: 8,
    alignItems: "center",
  },
  label: {
    fontSize: 14,
    color: "#FFF",
    fontWeight: 500,
    flex: 1,
  },
});
