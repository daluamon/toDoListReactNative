import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    paddingTop: 32,
    borderTopWidth: 1,
    marginHorizontal: 30,
    borderColor: "#333",
    justifyContent: "center",
    alignItems: "center",
  },
  label: {
    fontSize: 14,
    color: "#808080",
  },
  image: {
    marginTop: 24,
    marginBottom: 24,
  },
});
